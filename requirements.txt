# ── PyTorch CPU 전용 인덱스 (리눅스 빌드 시 CUDA 4.5GB 초과 방지) ─────────────
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.5.0; sys_platform == 'darwin'
torch==2.6.0+cpu; sys_platform != 'darwin'

# ── RAG 프레임워크 ──────────────────────────────
langchain>=0.3.0
langchain-community>=0.3.0
langchain-huggingface>=0.1.0
langchain-chroma>=0.2.0

# ── 벡터 데이터베이스 ──────────────────────────
chromadb>=0.5.0

# ── 임베딩 모델 & 트랜스포머 ────────────────────
sentence-transformers>=3.0.0
transformers>=4.44.0

# ── 상용 API 옵션 ──────────────────────────────
langchain-google-genai>=2.0.0

# ── 백엔드 API ─────────────────────────────────
fastapi>=0.115.0
uvicorn>=0.30.0

# ── 유틸리티 ───────────────────────────────────
python-dotenv>=1.0.0

# ── 크롤러 및 문서 파싱 (기본 라이브러리) ─────────
requests>=2.32.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# ── 문서 추출 및 OCR ─────────────────────────
pypdf>=5.0.0
pypdfium2>=4.30.0
olefile>=0.47
pillow>=10.0.0

